# Dev Container

Cross-cutting extension: reproducible VS Code dev container for any
scaffolded project.

## What it adds

- `.devcontainer/devcontainer.json` — Rust (bookworm) image with GitHub CLI,
  rust-analyzer + Even Better TOML for VS Code, and `cargo fetch` on create

## Compatibility

Applies to every template type.
