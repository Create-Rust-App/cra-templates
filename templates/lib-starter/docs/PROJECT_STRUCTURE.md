# Project structure

```text
src/
  main.rs                 # demo binary over the public API
  lib.rs                  # module root
  backoff.rs              # exponential backoff iterator
  slug.rs                 # slugify helper
  _feature_template.rs    # copy-paste starter for new features
tests/
  test_lib.rs             # integration tests over the public API
```
