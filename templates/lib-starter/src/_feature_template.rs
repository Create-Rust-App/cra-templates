//! Template for a new library feature module.
//!
//! Copy this file to `<feature>.rs`, declare it in `lib.rs`, and cover it in
//! `tests/test_lib.rs`. Delete this module if unused.

/// Placeholder feature entry point.
pub fn hello() -> &'static str {
    "feature ok"
}
