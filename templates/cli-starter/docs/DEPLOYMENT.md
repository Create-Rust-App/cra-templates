# Distribution

```sh
cargo build --release            # -> target/release/cli-starter
cargo install --path .           # install locally
```

Ship the single static binary; no runtime assets required.
