# Configuration

Environment variables (see `.env.example`):

| Variable | Default | Purpose |
|----------|---------|---------|
| `CLI_DEFAULT_NAME` | `world` | Name greeted when `--name` is omitted |
| `RUST_LOG` | `cli_starter=debug` | `tracing` filter directives |

Global flags: `--verbose` (repeatable) increases log verbosity.
