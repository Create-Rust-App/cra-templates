# Configuration

Environment variables (see `.env.example`):

| Variable | Default | Purpose |
|----------|---------|---------|
| `HOST` | `0.0.0.0` | Interface to bind |
| `PORT` | `3000` | TCP port |
| `RUST_LOG` | `leptos_starter=debug` | `tracing` filter directives |
