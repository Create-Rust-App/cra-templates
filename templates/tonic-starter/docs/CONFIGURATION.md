# Configuration

Environment variables (see `.env.example`):

| Variable | Default | Purpose |
|----------|---------|---------|
| `HOST` | `0.0.0.0` | Interface to bind |
| `PORT` | `50051` | TCP port |
| `RUST_LOG` | `tonic_starter=debug` | `tracing` filter directives |
